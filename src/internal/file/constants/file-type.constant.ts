export enum FILE_IMAGE_MIME {
  JPG = 'image/jpg',
  JPEG = 'image/jpeg',
  PNG = 'image/png',
}

export enum FILE_PDF_MIME {
  PDF = 'application/pdf',
}

export enum ZIP_PDF_MIME {
  PDF = 'application/zip',
}
